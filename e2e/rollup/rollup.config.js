// Basic rollup configuration for e2e testing
import { TEST_CONFIGURATIONS } from "./rollup-configs";

// Export the default configuration for the build script
export default TEST_CONFIGURATIONS.default;

