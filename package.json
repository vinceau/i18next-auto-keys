{
  "name": "i18next-icu-loader",
  "main": "dist/loader.js",
  "types": "dist/loader.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "test": "jest --runInBand",
    "test:watch": "jest --watch",
    "clean": "rimraf dist",
    "prepublishOnly": "npm run clean && npm run build"
  },
  "devDependencies": {
    "@types/jest": "^29",
    "@types/webpack": "^5",
    "esbuild-loader": "^4.4.0",
    "jest": "^29",
    "loader-utils": "^2.0.4",
    "memfs": "^4",
    "rimraf": "^6.0.1",
    "source-map": "^0.7.6",
    "ts-jest": "^29.4.5",
    "ts-loader": "^9.5.4",
    "ts-node": "^10.9.2",
    "typescript": "^5",
    "unionfs": "^4",
    "webpack": "^5"
  }
}
